{
  "name": "betterclaws",
  "version": "0.1.0",
  "description": "A lightweight, secure, self-hosted personal AI assistant gateway",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "node dist/src/index.js",
    "pretest": "npm run clean",
    "test": "npm run build && node --test 'dist/tests/**/*.test.js'",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
