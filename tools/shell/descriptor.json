{
  "name": "shell",
  "description": "Execute a shell command and return stdout, stderr, and exit code. Uses execFile for safety (no shell injection).",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "The executable to run (e.g., 'ls', 'grep', 'node')"
      },
      "args": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Arguments to pass to the command"
      },
      "cwd": {
        "type": "string",
        "description": "Working directory for the command. Defaults to the scratch directory."
      },
      "timeout": {
        "type": "number",
        "description": "Timeout in milliseconds. Defaults to the executor timeout."
      }
    },
    "required": ["command"]
  },
  "capabilities": ["exec:shell"]
}
