{
  "name": "file-read",
  "description": "Read the contents of a file. Supports optional line range (startLine/endLine) for partial reads.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Absolute path to the file to read"
      },
      "startLine": {
        "type": "number",
        "description": "First line to read (1-based, inclusive). Omit to start from the beginning."
      },
      "endLine": {
        "type": "number",
        "description": "Last line to read (1-based, inclusive). Omit to read to the end."
      }
    },
    "required": ["path"]
  },
  "capabilities": ["fs:read"]
}
