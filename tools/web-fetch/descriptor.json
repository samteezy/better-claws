{
  "name": "web-fetch",
  "description": "Fetch a URL via HTTP and return the status, headers, and body. Body is truncated if it exceeds maxBytes.",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to fetch"
      },
      "method": {
        "type": "string",
        "description": "HTTP method (GET, POST, etc.). Defaults to GET.",
        "enum": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD"]
      },
      "headers": {
        "type": "object",
        "description": "HTTP headers to send with the request"
      },
      "body": {
        "type": "string",
        "description": "Request body (for POST/PUT/PATCH)"
      },
      "maxBytes": {
        "type": "number",
        "description": "Maximum response body size in bytes. Defaults to 102400 (100KB)."
      }
    },
    "required": ["url"]
  },
  "capabilities": ["net:outbound"]
}
